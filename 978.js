function asd(numbers) {
    return numbers.reduce((acc, el) => acc + el ** 2, 0);

    /*return numbers.reduce((acc, el) => {
        return acc + el ** 2;
    }, 0)*/
}

function qwe() {
    console.log(asd([])); 
}

qwe();
